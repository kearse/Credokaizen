{
  "name": "credokaizen-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "validate:data": "npx ts-node scripts/validate-data.ts",
    "generate:sitemap": "npx ts-node scripts/generate-sitemap.ts",
    "generate:og": "npx ts-node scripts/generate-og.ts",
    "export": "npm run validate:data && npm run generate:sitemap && npm run generate:og && next build && next export"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^11.1.7",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18",
    "satori": "^0.10.14",
    "@resvg/resvg-js": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.3",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5",
    "ts-node": "^10.9.2"
  }
}